USE [TaskManagerApp]
GO

SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

BEGIN TRY
	BEGIN TRANSACTION;
/****** Modify AccessTime column to be DATETIME ******/

ALTER TABLE [dbo].[DeviceData]
ALTER COLUMN [AccessTime] DATETIME NOT NULL;

ALTER TABLE [dbo].[DeviceData]
DROP COLUMN [AccessCount];

COMMIT TRANSACTION;

END TRY

BEGIN CATCH
	IF @@TRANCOUNT > 0
		ROLLBACK TRANSACTION;
		THROW;
END CATCH